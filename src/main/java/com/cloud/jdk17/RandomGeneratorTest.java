package com.cloud.jdk17;import java.util.List;import java.util.random.RandomGenerator;import java.util.random.RandomGeneratorFactory;import java.util.stream.Collectors;/** * 随机数生成器统一由 RandomGeneratorFactory 生成 * 356：增强型伪随机数发生器 */public class RandomGeneratorTest {    public static void test() {        //生成10个10以内的随机数        RandomGeneratorFactory<RandomGenerator> L128X1024MixRandom = RandomGeneratorFactory.of("L64X128StarStarRandom");        RandomGenerator randomGenerator = L128X1024MixRandom.create(System.currentTimeMillis());        for (int i = 0; i < 10; i++) {            System.out.println(randomGenerator.nextInt(1000));        }    }    public static List<RandomGeneratorFactory<RandomGenerator>> all() {        return RandomGeneratorFactory.all().collect(Collectors.toList());    }}